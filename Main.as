package {
    import flash.display.Sprite;
    import com.sciprog.asynchrony.*;
    import com.sciprog.asynchrony.AsyncEvent;
    import com.sciprog.asynchrony.AsyncMethodToken;
    import com.sciprog.asynchrony.Asynchronous;
    import com.sciprog.asynchrony.Asynchrony;
    import com.sciprog.asynchrony.CallableMethod;
    import com.sciprog.asynchrony.MethodGroup;
    import com.sciprog.asynchrony.MethodInvoker;
    import com.sciprog.asynchrony.MethodSequence;
    import com.sciprog.asynchrony.callable;
    import com.sciprog.asynchrony.delay;
    import com.sciprog.asynchrony.group;
    import com.sciprog.asynchrony.sequence;

    public class Main extends Sprite {
        
        public function Main() {
            AsyncEvent;
            AsyncMethodToken;
            Asynchronous;
            Asynchrony;
            CallableMethod;
            MethodGroup;
            MethodInvoker;
            MethodSequence;
            callable;
            delay;
            group;
            sequence;
        }
    }
}
